/**
 * Ask runs full-bleed with no shell.
 *
 * The dark, centred stage is the whole point — a light sidebar beside it fights
 * the design and shrinks the one thing a dealer came here to use. Navigation
 * back to the yard lives in the corner of the page instead.
 */
export default function AskLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
